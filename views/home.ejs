<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8">
    <title>Panel de usuario - Home</title>
    <link rel="stylesheet" href="/login.css">
    <link rel="stylesheet" href="/home.css" />
    <link rel="stylesheet" href="/styles.css" />
    <style>
      .header-view {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 30px;
        padding: 0 20px;
      }
      .header-view h1 {
        color: #d4746a; /* Un color distintivo para el título */
        font-size: 28px;
        margin: 0;
        text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.1);
      }

      .edit-button {
        text-decoration: none;
        font-size: 16px;
        padding: 12px 20px;
      }

      .edit-button:hover {
        text-decoration: none;
      }
      .content {
        padding: 20px;
        max-width: 1200px;
        margin: 0 auto;
        text-align: center;
      }

      .main-title {
        color: #333; /* Color más neutro para el título principal */
        font-size: 36px;
        margin-bottom: 10px;
      }

      .description {
        color: #666;
        font-size: 18px;
        margin-bottom: 40px;
      }

      .grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: 25px;
        margin-bottom: 40px;
      }

      .card {
        background-color: #fff;
        border-radius: 12px;
        box-shadow: 0 6px 15px rgba(0, 0, 0, 0.08);
        overflow: hidden;
        transition: transform 0.3s ease, box-shadow 0.3s ease;
        text-align: center;
        text-decoration: none;
        color: inherit;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
      }

      .card:hover {
        transform: translateY(-8px);
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.15);
      }

      .card img {
        width: 100%;
        height: 180px; /* Altura fija para las imágenes */
        object-fit: cover;
        border-bottom: 1px solid #eee;
      }

      .card h2 {
        font-size: 24px;
        color: #d4746a;
        margin: 15px 0 5px;
        padding: 0 15px;
      }

      .card p {
        font-size: 16px;
        color: #666;
        padding: 0 15px 15px;
        margin-top: 0;
      }

      /* Botón de añadir lista */
      .add-list-button {
        background-color: #5cb85c; /* Color verde para "añadir" */
        color: white;
        padding: 15px 30px;
        border: none;
        border-radius: 8px;
        font-size: 18px;
        cursor: pointer;
        transition: background-color 0.3s ease;
        text-decoration: none;
        display: inline-block;
      }

      .add-list-button:hover {
        background-color: #4cae4c;
        text-decoration: none;
      }

      /* Header styles */
      .main-header {
        position: fixed;
        top: 0;
        left: 0;
        width: 100vw;
        z-index: 100;
        background: rgba(255,255,255,0.95);
        box-shadow: 0 2px 12px rgba(0,0,0,0.08);
        display: flex;
        align-items: center;
        justify-content: center;
        height: 90px;
      }
      .navbar {
        width: 100%;
        max-width: 1200px;
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 0 30px;
        height: 90px;
      }
      .logo-header {
        display: flex;
        align-items: center;
        gap: 15px;
      }
      .logo-header img {
        height: 60px;
        width: auto;
        border-radius: 15px;
        background: #fff;
        border: 2px solid #f4c2a1;
        box-shadow: 0 2px 8px rgba(244, 194, 161, 0.15);
      }
      .logo-header h1 {
        font-size: 28px;
        color: #d4746a;
        font-family: 'Gochi Hand', cursive;
        margin: 0;
      }
      #header-buttons {
        display: flex;
        align-items: center;
        gap: 10px;
      }
      @media (max-width: 600px) {
        .main-header, .navbar {
          height: 70px;
          padding: 0 10px;
        }
        .logo-header img {
          height: 40px;
        }
        .logo-header h1 {
          font-size: 18px;
        }
        #header-buttons {
          flex-direction: column;
          gap: 5px;
        }
      }

      /* Responsive design */
      @media (max-width: 768px) {
        .header-view {
          flex-direction: column;
          gap: 15px;
          text-align: center;
        }
        .grid {
          grid-template-columns: 1fr; /* Una sola columna en móviles */
        }
        .main-title {
            font-size: 28px;
        }
        .description {
            font-size: 16px;
        }
      }

      #header-buttons {
  display: flex;
  align-items: center;
  gap: 10px;
}
.btn {
  border: none;
  border-radius: 20px;
  font-size: 16px;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.3s ease;
  padding: 12px 25px;
  box-shadow: 0 5px 15px rgba(0,0,0,0.08);
}
.btn-success {
  background-color: #5cb85c;
  color: white;
  box-shadow: 0 8px 20px rgba(92, 184, 92, 0.3);
}
.btn-success:hover {
  background-color: #4cae4c;
  transform: translateY(-2px);
  box-shadow: 0 10px 25px rgba(76, 174, 76, 0.4);
}
.btn-danger {
  background-color: #d9534f;
  color: white;
  box-shadow: 0 5px 15px rgba(217, 83, 79, 0.2);
}
.btn-danger:hover {
  background-color: #c9302c;
  transform: translateY(-2px);
  box-shadow: 0 7px 20px rgba(201, 48, 44, 0.3);
}
    </style>
  </head>
  <body>
    <header class="main-header">
      <nav class="navbar">
        <div class="logo-header">
          <img src="/logo.png" alt="logo" />
          <h1>Gestor de Listas de Tareas</h1>
        </div>
        <div id="header-buttons">
          <button id="go-to-lists" class="btn btn-success" style="margin: 0 10px;">Ver mis listas</button>
          <button id="logout" class="btn btn-danger" style="margin: 0 10px;">Cerrar sesión</button>
        </div>
      </nav>
    </header>
    <div class="container" style="margin-top:110px;">
      <div class="content">
        <div class="intro-section" style="margin-bottom: 40px;">
          <h1 class="main-title">Bienvenido/a a tu Gestor de Listas de Tareas</h1>
          <p class="description">Organiza tus tareas, proyectos y listas de la forma más sencilla y visual. Crea listas, añade tareas y mantén tu día a día bajo control con un solo clic.</p>
        </div>
        <div id="new-list-form-container" class="form-container" style="max-width: 500px; margin: 0 auto; display: none;">
          <h3 style="color: #d4746a;">Crear nueva lista</h3>
          <form id="new-list-form" method="POST" action="/api/lists">
            <div class="form-group">
              <label for="nom">Nombre de la lista:</label>
              <input type="text" id="nom" name="nom" required>
            </div>
            <div class="form-group">
              <label for="descripcio">Descripción:</label>
              <textarea id="descripcio" name="descripcio" required style="min-height: 60px;"></textarea>
            </div>
            <button type="submit">Crear lista</button>
          </form>
          <span id="form-message"></span>
          <div id="detalle-lista" style="margin-top:20px; display:none;"></div>
        </div>
      </div>
    </div>
    <script>
      const logoutButton = document.getElementById("logout");
      const listsGrid = document.getElementById("lists-grid");
      const loadingMessage = document.getElementById("loading-message");
      const errorMessage = document.getElementById("error-message");

      // Manejar el cierre de sesión
      logoutButton?.addEventListener("click", async (e) => {
        e.preventDefault();
        try {
          const response = await fetch("/api/auth/logout", {
            method: "POST",
            headers: {
              "Content-Type": "application/json",
            },
          });
          if (response.ok) {
            window.location.href = "/"; // Redirigir a la página de inicio
          } else {
            alert("Error al cerrar sesión. Por favor, inténtalo de nuevo.");
          }
        } catch (error) {
          console.error("Error al cerrar sesión:", error);
          alert("Error de red al intentar cerrar sesión.");
        }
      });

      // Función para obtener y mostrar las listas
      const fetchAndDisplayLists = async () => {
        loadingMessage.style.display = 'block'; // Mostrar mensaje de carga
        errorMessage.style.display = 'none'; // Ocultar mensaje de error
        listsGrid.innerHTML = ''; // Limpiar el contenido actual del grid

        try {
          // Asumimos que el endpoint para obtener todas las listas es /api/lists
          const response = await fetch("/api/lists");
          if (!response.ok) {
            throw new Error(`HTTP error! status: ${response.status}`);
          }
          const lists = await response.json();

          if (lists.length === 0) {
            listsGrid.innerHTML = '<p>No tienes listas creadas aún. ¡Crea la primera!</p>';
          } else {
            lists.forEach((list) => {
              const card = document.createElement("a");
              card.href = `/lists/${list.id_list}`; // Usar id_list para el enlace
              card.classList.add("card");
              card.innerHTML = `
                <img src="/default-list-image.jpg" alt="${list.nom || 'Lista sin nombre'}" />
                <h2>${list.nom || 'Lista sin nombre'}</h2>
                <p>${list.tasks ? list.tasks.length : 0} tareas</p>
                <p>${list.descripcio || ''}</p> <!-- Mostrar la descripción si existe -->
              `;
              listsGrid.appendChild(card);
            });
          }
        } catch (error) {
          console.error("Error al cargar las listas:", error);
          errorMessage.style.display = 'block'; // Mostrar mensaje de error
          listsGrid.innerHTML = ''; // Limpiar el mensaje de carga si hay error
        } finally {
          loadingMessage.style.display = 'none'; // Ocultar mensaje de carga
        }
      };

      // Cargar las listas cuando el DOM esté completamente cargado
      document.addEventListener("DOMContentLoaded", () => {
        fetchAndDisplayLists();
        if (window.location.hash === "#nueva-lista") {
          document.getElementById('new-list-form-container').style.display = 'block';
          window.scrollTo({ top: document.getElementById('new-list-form-container').offsetTop - 30, behavior: 'smooth' });
        }
      });

      // Solo mostrar el botón 'Ver mis listas' si NO estamos en la vista de listas
      function updateHeaderButtons() {
        const hash = window.location.hash;
        const btnLists = document.getElementById('go-to-lists');
        if (hash === '#nueva-lista') {
          btnLists.style.display = 'inline-block';
        } else {
          btnLists.style.display = 'none';
        }
      }
      updateHeaderButtons();
      window.addEventListener('hashchange', updateHeaderButtons);
      document.getElementById('go-to-lists').onclick = () => {
        window.location.hash = '';
        updateHeaderButtons();
      };
      document.getElementById('show-new-list-form').onclick = () => {
        window.location.hash = 'nueva-lista';
        updateHeaderButtons();
      };
      // Envío por AJAX para evitar recarga
      const form = document.getElementById('new-list-form');
      const msg = document.getElementById('form-message');
      const detalleDiv = document.getElementById('detalle-lista');
      form?.addEventListener('submit', async (e) => {
        e.preventDefault();
        const nom = document.getElementById('nom').value;
        const descripcio = document.getElementById('descripcio').value;
        try {
          const res = await fetch('/api/lists', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ nom, descripcio })
          });
          if (res.ok) {
            const nuevaLista = await res.json();
            msg.innerText = 'Lista creada correctamente';
            msg.style.color = 'green';
            form.reset();
            detalleDiv.innerHTML = `<a href="/lists/${nuevaLista.id_list}" class="btn btn-primary" style="margin-top:15px;">Ver detalles de la nueva lista</a>`;
            detalleDiv.style.display = 'block';
          } else {
            const errorText = await res.text();
            msg.innerText = errorText || 'Error al crear la lista';
            msg.style.color = 'red';
            detalleDiv.style.display = 'none';
          }
        } catch (err) {
          msg.innerText = 'Error de conexión con el servidor';
          msg.style.color = 'red';
          detalleDiv.style.display = 'none';
        }
      });
    </script>
  </body>
</html>